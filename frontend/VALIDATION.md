# Validation

本项目已在当前环境执行：

```bash
npm install
npm run build
```

构建结果：成功。

构建产物：

```text
dist/index.html
dist/assets/index-*.css
dist/assets/index-*.js
```

说明：压缩包包含完整源码、配置文件、package-lock.json、README 和构建产物 dist；不包含 node_modules。
